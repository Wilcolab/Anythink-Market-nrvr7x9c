/**
 * Converts a string to camelCase format.
 * 
 * Takes any string with words separated by spaces, hyphens, or underscores
 * and converts it to camelCase where the first word is lowercase and each
 * subsequent word has its first letter capitalized with no separators.
 * 
 * @param {string} str - The input string to convert
 * @returns {string} The camelCase version of the input string
 * 
 * @example
 * toCamelCase('hello-world-example')
 * // Returns: 'helloWorldExample'
 * 
 * @example
 * toCamelCase('hello_world_example')
 * // Returns: 'helloWorldExample'
 * 
 * @example
 * toCamelCase('hello world example')
 * // Returns: 'helloWorldExample'
 */
function toCamelCase(str) {
    // Implementation here
}